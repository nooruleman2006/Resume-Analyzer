I have a Flask resume analyzer app. I need you to do the following tasks:

## TASK 1: Fix Groq API Integration (Backend)

In `services/ai_analyzer.py`, make these changes:

1. Add a `_get_groq_key()` function that reads `Config.GROQ_API_KEY`
2. Add a `_call_groq()` function that calls `https://api.groq.com/openai/v1/chat/completions` using bearer auth, trying these models in order: `llama3-70b-8192`, `llama3-8b-8192`, `mixtral-8x7b-32768`
3. Change `generate_interview_questions()` to use Groq FIRST, fall back to Gemini only if Groq fails
4. Change `evaluate_interview_answer()` to use Groq FIRST, fall back to Gemini only if Groq fails
5. Keep `analyze_resume()` using Gemini only (it handles PDF/document understanding)

## TASK 2: Fix Config (config.py)

Add this line inside the Config class:
```python
GROQ_API_KEY = os.environ.get('GROQ_API_KEY', '')
```

## TASK 3: Fix Frontend (templates/interview_prep.html)

In the JavaScript section that handles the `/interview-prep/generate` AJAX response:
1. Find where it checks `data.error === 'rate_limit'` and displays a message
2. Replace any hardcoded "Gemini free-tier rate limit hit (15 req/min)" text with: "Rate limit hit. Auto-retrying in Xs..."
3. The retry countdown should use `data.retry_after` value from the server response

## TASK 4: Add Groq Secret in Replit

In Replit Secrets (not .env file), add:
- Key: `GROQ_API_KEY`
- Value: (I will add this manually)

Make sure `config.py` reads it via `os.environ.get('GROQ_API_KEY', '')`

## TASK 5: Clean Up Unnecessary Files

Delete these files/folders if they exist:
- `.agents` folder and `agents.zip`
- `__pycache__` folders everywhere (all nested ones too)
- `resume_analyzer.db` (gets recreated automatically)
- `venv` folder (Replit manages its own packages)
- Any `.pyc` files anywhere in the project
- `resume.sql` if the app uses SQLAlchemy or creates DB automatically

Run this cleanup command:
```bash
find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null
find . -name "*.pyc" -delete 2>/dev/null
rm -rf venv .agents agents.zip resume_analyzer.db 2>/dev/null
```

## TASK 6: Verify Everything Works

1. Print to console on startup:
```python
print("GROQ configured:", bool(Config.GROQ_API_KEY))
print("GEMINI configured:", bool(Config.GEMINI_API_KEY))
```

2. Make sure `requirements.txt` includes `requests` (needed for Groq API calls)

## Summary of API responsibilities after fix:
- Gemini API → resume analysis only (`analyze_resume`)  
- Groq API → interview question generation + answer evaluation
- If Groq fails for any reason → automatically falls back to Gemini
- Rate limit errors should show generic message, not mention Gemini specifically

## Important Notes:
- Do NOT use any Groq SDK/library — use raw `requests` HTTP calls only
- Groq API key will be in Replit Secrets as `GROQ_API_KEY`
- Gemini API key is already in Replit Secrets as `GEMINI_API_KEY`
- Do not touch any other routes or services unrelated to interview prep