My resume analyzer has multiple critical bugs. Fix all of them:

BUG 1 — Wrong Missing Keywords:
In services/ai_analyzer.py, the Gemini prompt is returning programming 
keywords (python, java, javascript, c++, rust, php) for ALL resumes 
regardless of field. Fix the prompt to:
- First detect the profession/industry from the resume text
- Then suggest ONLY field-relevant missing keywords
- For Electrical Engineering resumes suggest keywords like: AutoCAD, 
  MATLAB, PLC, SCADA, circuit design, power systems, transformer, 
  relay protection, load flow, Proteus, ETAP, HMI, switchgear, 
  electrical drawings, IEC standards
- Never suggest programming languages unless the resume is actually 
  for a software role

BUG 2 — Gemini AI not responding / AI feedback unavailable:
In services/ai_analyzer.py, the Gemini API call is failing silently 
and returning fallback messages. Fix this by:
- Adding proper error logging to see why Gemini is failing
- Making sure GEMINI_API_KEY is being read correctly from environment
- Using the correct Gemini model name: "gemini-1.5-flash"
- Adding retry logic if the API call fails once
- Making sure the response is parsed correctly and all fields 
  (suitable_roles, strengths, weaknesses, suggestions) are populated

BUG 3 — Suitable Roles not showing:
Instead of real role suggestions, it shows "Review your resume", 
"Upload again when AI is available", "Check the ATS score below".
These are fallback placeholder texts. Fix the Gemini prompt to always 
return 3 suitable roles based on the actual resume content and field.
For Electrical Engineering it should return roles like:
"Electrical Engineer", "Power Systems Engineer", "Electrical Design 
Engineer", "Protection Engineer", "Instrumentation Engineer"

BUG 4 — Skills Detection is wrong:
Skills detected are only showing soft skills (communication, problem, 
analytical, management, collaboration) for every resume.
Fix services/ats_scorer.py to:
- Detect technical skills specific to the resume's field
- For Electrical Engineering detect: AutoCAD, MATLAB, PLC, SCADA, 
  circuit design, power systems, Proteus, ETAP etc.
- Only show soft skills if no technical skills are found

BUG 5 — Auto Apply works without a resume:
In routes/apply_routes.py, add a check at the start:
- If user has no uploaded resume, redirect them to the upload page 
  with an error message: "Please upload a resume before using 
  Auto Apply"
- Do not allow auto apply to proceed without a resume

BUG 6 — ATS Score is inaccurate:
In services/ats_scorer.py, the ATS scoring is not field-aware.
Fix it to:
- Detect the profession from resume text first
- Score keywords match based on field-specific keywords, not generic ones
- Skills Match score should reflect actual technical skills present 
  in the resume for that specific field

Please fix all 6 bugs and test with an Electrical Engineering resume 
to verify the fixes work correctly.